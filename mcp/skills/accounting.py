"""
mcp/skills/accounting.py — Ledger query skills (stub).

Skills planned:
    get_balance_sheet(year)       → Balance Sheet dict
    get_income_statement(year)    → Income Statement dict
    get_general_ledger(year)      → GL entries list
    get_trial_balance(year)       → Trial balance dict
    get_account_balance(acct, yr) → single account balance
"""
# TODO: implement in Task 5 using ledger.stmtBS, stmtIS, stmtGL, ledgerDB
