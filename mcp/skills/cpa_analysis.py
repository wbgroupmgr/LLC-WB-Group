"""
mcp/skills/cpa_analysis.py — CPA analysis skills (stub).

Skills planned:
    analyze_cash_flow(year)        → cash flow memo
    compute_ratios(year)           → liquidity / coverage ratios
    flag_anomalies(year)           → list of flagged entries
    generate_cpa_memo(year)        → narrative memo string
"""
# TODO: implement in Task 5 using ledger statements + AI narrative generation
